
services:
  db:
    image: mysql:9.1.0
    container_name: hormiga-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: hormiga_finanzas
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: